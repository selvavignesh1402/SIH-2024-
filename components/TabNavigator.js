import React from 'react';
import { Tabs } from 'expo-router';
import _layout from '../app/(tabs)/_layout';  // Assuming you have a custom TabBar component

const TabNavigator = () => {
  return (
    <_layout/>
  );
};

export default TabNavigator;
